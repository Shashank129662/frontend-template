# Frontend assets

This directory is the canonical location for supplied frontend assets. Use only `.webp` for visual raster assets. Place files in `backgrounds/`, `images/`, `icons/`, `logos/`, or `fonts/` according to purpose. Fonts may use their native webfont format when supplied.
